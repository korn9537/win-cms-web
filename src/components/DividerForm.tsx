import { Divider, Grid } from '@mui/material';
import React from 'react';

export default function DividerForm() {
  return (
    <Grid item xs={12} style={{ paddingTop: '32px', paddingBottom: '32px' }}>
      <Divider />
    </Grid>
  );
}
